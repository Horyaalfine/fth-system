{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python models/db.py && gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT --timeout 120 --preload",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}